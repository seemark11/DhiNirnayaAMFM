# Regression Model Training and Evaluation

This repository contains a Python script to train and evaluate regression models on a dataset with MMSE scores. The script supports **Decision Tree Regressor** and **Support Vector Regressor (SVR)** with **hyperparameter tuning via GridSearchCV**, model saving, and performance evaluation.

---


